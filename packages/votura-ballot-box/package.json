{
  "name": "@repo/votura-ballot-box",
  "version": "0.0.0",
  "description": "A package intended for generating, decrypting and tallying ballot papers in the JSON format votura uses.",
  "main": "dist/index.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/SE-UUlm/votura.git"
  },
  "type": "module",
  "engines": {
    "node": ">=22.15.0"
  },
  "scripts": {
    "format": "prettier --write .",
    "check-format": "prettier --check .",
    "check-types": "tsc --noEmit",
    "lint": "eslint",
    "build": "tsc --project tsconfig.build.json"
  },
  "dependencies": {
    "dotenv": "^17.2.0",
    "@votura/votura-crypto": "^0.0.3",
    "@repo/votura-validators": "*"
  },
  "devDependencies": {
    "@eslint/compat": "^1.3.0",
    "@repo/eslint-config": "*",
    "@repo/typescript-config": "*",
    "eslint": "^9.25.1",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2",
    "typescript-eslint": "^8.31.0",
    "tsx": "^4.19.4",
    "vitest": "^3.2.4"
  },
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./dist/index.js"
    }
  }
}