components:
  responses:
    electionNotFound:
      description: |
        Not Found.
        The requested election was not found.
      content:
        application/json:
          schema:
            type: 'object'
            properties:
              error:
                type: 'string'
                example: 'Election not found'
